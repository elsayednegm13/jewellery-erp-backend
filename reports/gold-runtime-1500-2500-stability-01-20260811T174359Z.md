# GOLD-RUNTIME-1500-2500-STABILITY-01

## القرار والنطاق

- Owner-approved refresh: `1500` seconds.
- Owner-approved stale threshold: `2500` seconds.
- Provider: `GOLDAPI_IO`; mode: `LIVE_PROVIDER`; currency: `AED`.
- CGP policy observed: `BID / NONE / 0`.
- No provider switch, manual mode, CGP-policy change, business transaction,
  migration, server connection, deployment, or Next restart was performed.
- `GoldMarketSetting` was read-only inspected. This batch did not update it.

## Runtime

- Backend listener: PID `11732`, port `8000`, inherited owner-restarted local
  process; no restart or touch by this batch.
- Frontend listener: port `3000`, inherited Next development process; not
  started or restarted.
- Redis: container `darfus-redis`, port `6379`, `PING=PONG`, backend health
  connected.
- PostgreSQL: local listener `5432`.

## Settings and scheduler

Read-only `SELECT current_database()` returned `darfus_erp`.

Current canonical setting:

- refresh interval: `1500`
- stale threshold: `2500`
- provider/mode/currency: `GOLDAPI_IO / LIVE_PROVIDER / AED`
- enabled: `true`

BullMQ queue `gold-market-refresh` has exactly one scheduler:

`gold-market-refresh:COMP-1384c23f-18ee-405f-8675-8e87746be72c:GOLDAPI_IO:AED:XAU`

- `every=1500000ms` (`1500s`)
- `OLD_30_SECOND_GOLD_REPEAT_JOB_COUNT=0`
- duplicate Gold scheduler count: `0`
- targeted cleanup: not needed
- Redis global mutation: `NO`

## Provider and quote

- Secret configuration was checked by presence only; the secret value was not
  printed or changed.
- One bounded direct request to `/api/XAU/AED` returned HTTP `200` with valid
  XAU/AED price, BID, and ASK fields.
- Latest canonical quote: provider `GOLDAPI_IO`, currency `AED`, unit
  `PER_GRAM`, quote timestamp `2026-08-11T17:33:03.000Z`, received at
  `2026-08-11T17:33:05.534Z`.
- Final observed quote age: approximately `553.679s`, below `2500s`; status
  `FRESH`/`HEALTHY`.
- BID `516.94154976`, SPOT `516.97579352`, ASK `517.02892585`; all canonical
  values valid.
- `/api/v1/health/gold` returned HTTP `200`, canonical provider-neutral state,
  `LIVE_PROVIDER`, `isMockFallback=false`, and no provider HTTP call in the
  health implementation.

Effective CGP rates from canonical current state:

- 18K `387.7062`
- 21K `452.3239`
- 22K `473.8631`
- 24K `516.9415`

CGP posting remains provider-neutral; `cgp-posting.service.js` resolves the
stored quote/policy and does not call GoldAPI directly.

## 30s/120s forensic result

Production code contains defensive defaults of `30`/`120` for missing settings
and unit-test fixtures. The canonical enabled persistent setting is present and
is passed through runtime, health, Gold Center reference, and CGP paths as
`1500`/`2500`; no default overrides that active setting. The dashboard's
30-second React refetch is an API poll and does not call GoldAPI directly.

Provider-call entrypoints found:

1. Canonical BullMQ `gold-market-refresh` worker → provider adapter.
2. Explicit administrator Test Connection endpoint.
3. Legacy compatibility `/gold/live` and `gold-sync` queue path in
   `gold.service.js` (separate legacy surface; not used by CGP posting).

Frontend direct GoldAPI references: `0`.

During a bounded 90-second observation (three 30-second samples), quote count
remained `98` and the latest quote remained unchanged. No 30-second provider
request storm was observed.

Estimated scheduled consumption for one logical job:
`30 days * 86400 / 1500 = approximately 1728 requests/month`, plus explicit
Test Connection and any manually-triggered refreshes.

## Boundary and regressions

- Freshness unit boundary: age `2499s` accepted; age `2501s` rejected;
  `CGP_2500S_STALE_BOUNDARY_REGRESSION=PASS`.
- Canonical Gold runtime/health/provider/policy tests: `26/26 PASS`.
- Gold making-charge/POS contract: `PASS`.
- Gold making-charge clone acceptance: `PASS`; integrity unbalanced journals,
  orphan lines, and unlinked treasury all `0`.
- Legacy missing-selling-rate pure guard: `PASS`. The historical reproduction
  script itself is obsolete against the current compatibility contract because
  the cloned asset has a valid persisted sale price; it failed to reproduce
  the old defect without touching any persistent database.
- Dashboard/Gold Center canonical SPOT and no-double-purity contract: `PASS`.
- TypeScript: `PASS`.
- `git diff --check`: `PASS` (only inherited CRLF warnings).

## Persistent read-only fingerprint

Persistent database remained `darfus_erp` throughout. Current counts:

- migrations `80`; migration `81` absent
- Assets `53`; Products `3`; Customers `1`; CGPs `2`
- Invoices `13`; Journals `67`; JournalLines `176`; CashTransactions `50`
- GoldMarketQuotes `98`; GoldMarketSettings `1`; pricing policies `1`

The earlier read-only snapshot had `90` quotes; the increase to `98` is
legitimate live-worker quote activity. No synthetic business transaction was
created.

Integrity at final read-only check:

- unbalanced journals `0`
- orphan journal lines `0`
- unlinked treasury `0`
- duplicate journal sources `0`
- duplicate treasury journal links `0`
- duplicate barcodes `0`
- empty barcodes `0`
- open cash-register sessions `1`
- signed GL: Cash `0.003`; Bank `20416.405`

Settings values were `1500/2500` before and after this batch; no settings
mutation was issued by this batch. No journal, treasury, asset, login,
provider, pricing-policy, date-control, or numeral-format mutation was issued.

## Safety and worktree

- branch `main`; HEAD `1657b0e9ba580faef69be48f04637835c201b521`
- inherited worktree changes preserved; no staging, commit, push, reset,
  restore, clean, or stash command used
- `next-env.d.ts` SHA remained the inherited known drift
  `7AD303E40D4FDDF44F156129E397511953A71481C5CFD86B1862649AAAF240CC`;
  it was not mutated or auto-repaired
- server connections/mutations/deployments: `0`

## Gate

All critical Live Gold 1500/2500 runtime conditions passed. The historical
POS reproduction command's obsolete expectation was not used as a runtime
stability failure; the current pure guard and clone acceptance both pass.

`GOLD_RUNTIME_1500_2500_STABILITY_01_GATE = PASS_CONFIRMED`

Next task is not started automatically. Handoff may record the accepted
1500/2500 runtime state and next task `SYSTEM-DATE-CONTROLS-COMPLETION-01`.
